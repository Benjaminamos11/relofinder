---
// Swiss regions with their key cities and services
const regions = [
  {
    id: "zurich",
    name: "Zürich",
    title: "Switzerland's Financial Capital",
    description: "Dynamic business hub with lake views and Alpine backdrop",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=Zurich",
    href: "/regions/zurich",
    stats: {
      population: "402,762",
      avgRent: "CHF 2,200",
      language: "German",
      industry: "Finance & Tech"
    }
  },
  {
    id: "geneva",
    name: "Geneva",
    title: "International Diplomacy Center",
    description: "Sophisticated lakeside city with global organizations",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=Geneva",
    href: "/regions/geneva",
    stats: {
      population: "201,818",
      avgRent: "CHF 2,500",
      language: "French",
      industry: "International Orgs"
    }
  },
  {
    id: "basel",
    name: "Basel",
    title: "Cultural & Pharmaceutical Hub",
    description: "Rhine-side city known for art, culture, and life sciences",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=Basel",
    href: "/regions/basel",
    stats: {
      population: "173,232",
      avgRent: "CHF 1,800",
      language: "German",
      industry: "Pharmaceuticals"
    }
  },

  {
    id: "lausanne",
    name: "Lausanne",
    title: "Olympic Capital",
    description: "Vibrant university city on Lake Geneva's shores",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=Lausanne",
    href: "/regions/lausanne",
    stats: {
      population: "139,111",
      avgRent: "CHF 1,700",
      language: "French",
      industry: "Education & Sports"
    }
  },
  {
    id: "lucerne",
    name: "Lucerne",
    title: "Gateway to Central Switzerland",
    description: "Picturesque lakeside town with mountain access",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=Lucerne",
    href: "/regions/lucerne",
    stats: {
      population: "82,620",
      avgRent: "CHF 1,500",
      language: "German",
      industry: "Tourism"
    }
  },
  {
    id: "zug",
    name: "Zug",
    title: "Crypto Valley",
    description: "Business-friendly canton with blockchain innovation",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=Zug",
    href: "/regions/zug",
    stats: {
      population: "30,542",
      avgRent: "CHF 2,000",
      language: "German",
      industry: "Blockchain & Finance"
    }
  },
  {
    id: "winterthur",
    name: "Winterthur",
    title: "Industrial Heritage City",
    description: "Technology and cultural center near Zürich",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=Winterthur",
    href: "/regions/winterthur",
    stats: {
      population: "114,220",
      avgRent: "CHF 1,400",
      language: "German",
      industry: "Technology"
    }
  },
  {
    id: "neuchatel",
    name: "Neuchâtel",
    title: "Watchmaking Capital",
    description: "Historic city famous for precision timepieces",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=Neuchatel",
    href: "/regions/neuchatel",
    stats: {
      population: "33,455",
      avgRent: "CHF 1,300",
      language: "French",
      industry: "Watchmaking"
    }
  },
  {
    id: "fribourg",
    name: "Fribourg",
    title: "Bilingual Heritage City",
    description: "Medieval town bridging German and French cultures",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=Fribourg",
    href: "/regions/fribourg",
    stats: {
      population: "38,039",
      avgRent: "CHF 1,200",
      language: "German/French",
      industry: "Education"
    }
  },
  {
    id: "valais",
    name: "Valais",
    title: "Alpine Paradise",
    description: "Stunning mountain region with world-class skiing",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=Valais",
    href: "/regions/valais",
    stats: {
      population: "348,503",
      avgRent: "CHF 1,100",
      language: "French/German",
      industry: "Tourism"
    }
  },
  {
    id: "gstaad",
    name: "Gstaad",
    title: "Luxury Alpine Resort",
    description: "Exclusive mountain destination for discerning residents",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=Gstaad",
    href: "/regions/gstaad",
    stats: {
      population: "3,400",
      avgRent: "CHF 3,500",
      language: "German",
      industry: "Luxury Tourism"
    }
  },
  {
    id: "st-moritz",
    name: "St. Moritz",
    title: "Alpine Luxury Destination",
    description: "World-renowned resort town in the Engadin Valley",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=St+Moritz",
    href: "/regions/st-moritz",
    stats: {
      population: "5,067",
      avgRent: "CHF 4,000",
      language: "German/Romansh",
      industry: "Luxury Tourism"
    }
  },
  {
    id: "andermatt",
    name: "Andermatt",
    title: "Alpine Village Resort",
    description: "Charming mountain village with year-round recreation",
    image: "https://via.placeholder.com/1200x800/e5e7eb/6b7280?text=Andermatt",
    href: "/regions/andermatt",
    stats: {
      population: "1,550",
      avgRent: "CHF 2,800",
      language: "German",
      industry: "Tourism"
    }
  }
];

// Initial active region (first one)
const defaultRegion = regions[0];
---

<div class="py-12 bg-gray-50">
  <div class="container">
    <h2 class="text-3xl font-bold text-center mb-8">Choose a <span class="text-coral-500">Region</span></h2>
    
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 items-start">
      <!-- Left: Region Selector -->
      <div class="bg-white rounded-xl shadow-sm overflow-hidden border border-gray-100">
        <div class="p-5 border-b border-gray-100">
          <div class="flex items-center text-lg font-semibold text-gray-800">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-coral-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
            </svg>
            Choose a Region
          </div>
        </div>
        
        <div class="region-list">
          {regions.map((region, index) => (
            <button 
              class={"w-full text-left py-4 px-5 border-b last:border-b-0 border-gray-100 hover:bg-gray-50 transition-colors " + (index === 0 ? 'bg-primary-50 text-coral-600 font-medium' : 'text-gray-700')}
              data-region-id={region.id}
            >
              {region.name}
            </button>
          ))}
        </div>
      </div>
      
      <!-- Right: Region Details -->
      <div class="lg:col-span-2">
        {regions.map((region) => (
          <div 
            class={"region-detail bg-white rounded-xl shadow-sm overflow-hidden border border-gray-100 " + (region.id === defaultRegion.id ? 'block' : 'hidden')}
            id={`region-${region.id}`}
          >
            <!-- Region Image -->
            <div class="relative h-64 overflow-hidden">
              <img 
                src={region.image}
                alt={region.name + " Region"}
                class="w-full h-full object-cover"
              />
              <div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/70 to-transparent p-4">
                <span class="inline-block bg-white/90 text-coral-600 text-xs font-medium px-2.5 py-1 rounded-full">
                  Region
                </span>
                <h3 class="text-3xl font-bold text-white mt-2">{region.name}</h3>
              </div>
            </div>
            
            <!-- Region Content -->
            <div class="p-6">
              <p class="text-accent-600 mb-6">
                {region.description}
              </p>
              
              <div class="mb-6">
                <h4 class="text-lg font-semibold mb-3">Key Services in {region.name}:</h4>
                <ul class="space-y-2">
                  {region.services.map(service => (
                    <li class="flex items-start">
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-coral-500 mr-2 mt-0.5 flex-shrink-0" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                      </svg>
                      {service}
                    </li>
                  ))}
                </ul>
              </div>
              
              <a 
                href={`/services?region=${region.id}`}
                class="inline-flex items-center px-5 py-2.5 bg-coral-500 text-white font-medium rounded-lg hover:bg-coral-600 transition-colors"
              >
                Explore {region.name} Services
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" viewBox="0 0 20 20" fill="currentColor">
                  <path fill-rule="evenodd" d="M10.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L12.586 11H5a1 1 0 110-2h7.586l-2.293-2.293a1 1 0 010-1.414z" clip-rule="evenodd" />
                </svg>
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('astro:page-load', () => {
    // Get all region buttons and detail panels
    const regionButtons = document.querySelectorAll<HTMLButtonElement>('.region-list button');
    const regionDetails = document.querySelectorAll<HTMLDivElement>('.region-detail');
    let currentRegionIndex = 0;
    let autoRotationTimer: number | undefined;
    let isPaused = false;
    
    // Function to activate a specific region
    function activateRegion(regionId: string, buttonElement: HTMLButtonElement | null = null) {
      // Update button styles
      regionButtons.forEach(btn => {
        btn.classList.remove('bg-primary-50', 'text-coral-600', 'font-medium');
        btn.classList.add('text-gray-700');
      });
      
      if (buttonElement) {
        buttonElement.classList.add('bg-primary-50', 'text-coral-600', 'font-medium');
        buttonElement.classList.remove('text-gray-700');
      } else {
        // Find the button by region ID
        const button = document.querySelector<HTMLButtonElement>(`.region-list button[data-region-id="${regionId}"]`);
        if (button) {
          button.classList.add('bg-primary-50', 'text-coral-600', 'font-medium');
          button.classList.remove('text-gray-700');
        }
      }
      
      // Show the selected region detail and hide others
      regionDetails.forEach(detail => {
        if (detail.id === `region-${regionId}`) {
          detail.classList.remove('hidden');
          detail.classList.add('block');
        } else {
          detail.classList.add('hidden');
          detail.classList.remove('block');
        }
      });
      
      // Update current index for auto rotation
      regionButtons.forEach((btn, index) => {
        if (btn.getAttribute('data-region-id') === regionId) {
          currentRegionIndex = index;
        }
      });
    }
    
    // Start auto rotation
    function startAutoRotation() {
      // Clear any existing timer
      if (autoRotationTimer) {
        clearInterval(autoRotationTimer);
      }
      
      // Set new timer for rotation every 3 seconds
      autoRotationTimer = window.setInterval(() => {
        if (!isPaused) {
          // Move to next region
          currentRegionIndex = (currentRegionIndex + 1) % regionButtons.length;
          const nextRegionId = regionButtons[currentRegionIndex].getAttribute('data-region-id') || '';
          activateRegion(nextRegionId);
        }
      }, 3000);
    }
    
    // Add click event to each region button
    regionButtons.forEach(button => {
      // Click handling
      button.addEventListener('click', () => {
        const regionId = button.getAttribute('data-region-id') || '';
        activateRegion(regionId, button);
        
        // Reset the timer when a user manually selects a region
        startAutoRotation();
      });
      
      // Hover handling
      button.addEventListener('mouseenter', () => {
        isPaused = true;
        const regionId = button.getAttribute('data-region-id') || '';
        activateRegion(regionId, button);
      });
      
      button.addEventListener('mouseleave', () => {
        isPaused = false;
      });
    });
    
    // Pause rotation when hovering over region details
    regionDetails.forEach(detail => {
      detail.addEventListener('mouseenter', () => {
        isPaused = true;
      });
      
      detail.addEventListener('mouseleave', () => {
        isPaused = false;
      });
    });
    
    // Initialize auto rotation
    startAutoRotation();
  });
</script>

<style>
  .regions-container {
    height: 280px;
    position: relative;
  }
  
  .regions-scroller {
    position: absolute;
    width: 100%;
    transition: transform 0.1s linear;
  }
  
  @media (min-width: 768px) {
    .regions-container {
      height: 320px;
    }
  }
</style> 