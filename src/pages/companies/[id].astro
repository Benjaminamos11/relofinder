---
import Layout from '../../layouts/Layout.astro';
import SEOHead from '../../components/common/SEOHead.astro';
import SchemaMarkup from '../../components/common/SchemaMarkup.astro';
import { getCollection } from 'astro:content';
import { marked } from 'marked';
import { fetchGoogleReviews, getMockReviews, type FormattedReview, type BusinessReviewData } from '../../utils/google-reviews';

// Generate static paths for all companies
export async function getStaticPaths() {
  const companies = await getCollection('companies');
  return companies.map((company: any) => ({
    params: { id: (company as any).data.id },
    props: { company },
  }));
}

// Get company data from props
const { company } = Astro.props;
const companyData = (company as any).data;

// Get related services and regions
const services = await getCollection('services');
const regions = await getCollection('regions');

const companyServices = services.filter((service: any) => 
  companyData.services.includes(service.data.id)
);

const companyRegions = regions.filter((region: any) => 
  companyData.regions.includes(region.data.id)
);

// Content processing
const { Content } = await company.render();

// Reviews data
const mockReviews = getMockReviews();
let reviewsData: BusinessReviewData = {
  rating: 4.5,
  reviewCount: 50,
  reviews: mockReviews,
  businessInfo: {
    address: '',
    placeId: ''
  }
};

try {
  const apiKey = import.meta.env.SERPAPI_KEY;
  if (apiKey && companyData.googleMyBusinessUrl) {
    const fetchedReviews = await fetchGoogleReviews(companyData.googleMyBusinessUrl, apiKey);
    if (fetchedReviews) {
      reviewsData = fetchedReviews;
    }
  }
} catch (error) {
  console.log('Error fetching Google reviews:', error);
}

const actualRating = companyData.rating?.score || reviewsData.rating || 4.5;
const actualReviewCount = companyData.rating?.reviews || reviewsData.reviewCount || 50;
const reviewsToDisplay = reviewsData.reviews || [];

// SEO setup
const title = `${companyData.name} | Swiss Relocation Services | ReloFinder.ch`;
const description = `${companyData.description} ⭐ ${actualRating}/5 rating from ${actualReviewCount} reviews. Expert relocation services in Switzerland.`;

// FAQ Data for better SEO and LLM optimization
const faqData = [
  {
    question: `What services does ${companyData.name} offer?`,
    answer: `${companyData.name} specializes in comprehensive Swiss relocation services including ${companyData.services.slice(0, 3).join(', ')} and more. We provide end-to-end support for your move to Switzerland.`
  },
  {
    question: `Which regions in Switzerland does ${companyData.name} cover?`,
    answer: `We provide relocation services across ${companyData.regions.join(', ')} and other major Swiss cities. Our local expertise ensures smooth transitions wherever you're moving in Switzerland.`
  },
  {
    question: `What makes ${companyData.name} different from other relocation companies?`,
    answer: `With a ${actualRating}/5 star rating from ${actualReviewCount} clients, ${companyData.name} combines ${companyData.founded ? `${new Date().getFullYear() - companyData.founded} years of` : ''} experience with personalized service. We handle everything from visa applications to finding your perfect home.`
  },
  {
    question: `How much do ${companyData.name}'s services cost?`,
    answer: `${companyData.name} offers competitive pricing for Swiss relocation services. Contact us for a free consultation and personalized quote based on your specific needs and timeline.`
  }
];
---

<Layout>
  <SEOHead 
    title={title}
    description={description}
    canonical={`https://relofinder.ch/companies/${companyData.id}`}
    openGraph={{
      title: title,
      description: description,
      image: companyData.logo,
      type: "article"
    }}
    keywords={[
      companyData.name.toLowerCase(),
      'swiss relocation',
      'switzerland moving',
      'relocation services',
      ...companyData.services,
      ...companyData.regions
    ]}
  />

  <SchemaMarkup 
    type="company"
    data={{
      "@type": "LocalBusiness",
      "name": companyData.name,
      "description": companyData.description,
      "url": `https://relofinder.ch/companies/${companyData.id}`,
      "logo": companyData.logo,
      "address": {
        "@type": "PostalAddress",
        "addressCountry": "CH"
      },
      "aggregateRating": {
        "@type": "AggregateRating",
        "ratingValue": actualRating,
        "reviewCount": actualReviewCount
      }
    }}
  />

  <!-- Hero Section -->
  <section class="relative min-h-screen bg-white overflow-hidden">
    <!-- Light gradient overlay -->
    <div class="absolute inset-0 bg-gradient-to-br from-gray-50 via-white to-gray-100"></div>
    
    <!-- Content -->
    <div class="relative container mx-auto px-4 py-20">
      <div class="grid lg:grid-cols-3 gap-12 items-center min-h-[80vh]">
        
        <!-- Left Column: Company Info -->
        <div class="lg:col-span-2">
          <div class="flex items-center gap-6 mb-8">
            <div class="w-24 h-24 bg-white rounded-2xl p-4 flex items-center justify-center shadow-lg border border-gray-200">
              <img src={companyData.logo} alt={`${companyData.name} logo`} class="w-full h-full object-contain" />
            </div>
            <div>
              <h1 class="text-5xl font-bold mb-2 text-gray-900">
                {companyData.name}
              </h1>
              <div class="flex items-center gap-4">
                <div class="flex items-center gap-2">
                  <div class="flex text-yellow-500">
                    {Array.from({length: 5}, (_, i) => (
                      <svg class={`w-6 h-6 ${i < Math.floor(actualRating) ? 'fill-current' : 'fill-gray-300'}`} viewBox="0 0 20 20">
                        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                      </svg>
                    ))}
                  </div>
                  <span class="text-xl font-semibold text-gray-900">{actualRating}/5</span>
                  <span class="text-gray-600">({actualReviewCount} reviews)</span>
                </div>
                {companyData.verified && (
                  <div class="flex items-center gap-2 bg-green-100 px-3 py-1 rounded-full border border-green-200">
                    <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                    <span class="text-green-600 font-medium">Verified</span>
                  </div>
                )}
              </div>
            </div>
          </div>

          <p class="text-xl text-gray-700 mb-8 leading-relaxed">{companyData.description}</p>

          <!-- Key Features -->
          <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mb-8">
            <div class="text-center">
              <div class="w-16 h-16 mx-auto mb-3 bg-blue-50 rounded-2xl flex items-center justify-center border border-blue-100">
                <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                </svg>
              </div>
              <div class="text-2xl font-bold text-gray-900">{companyData.regions.length}</div>
              <div class="text-gray-600">Regions</div>
            </div>
            <div class="text-center">
              <div class="w-16 h-16 mx-auto mb-3 bg-purple-50 rounded-2xl flex items-center justify-center border border-purple-100">
                <svg class="w-8 h-8 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2-2v2m8 0V6a2 2 0 012 2v6a2 2 0 01-2 2H8a2 2 0 01-2-2V8a2 2 0 012-2V6"/>
                </svg>
              </div>
              <div class="text-2xl font-bold text-gray-900">{companyData.services.length}</div>
              <div class="text-gray-600">Services</div>
            </div>
            <div class="text-center">
              <div class="w-16 h-16 mx-auto mb-3 bg-green-50 rounded-2xl flex items-center justify-center border border-green-100">
                <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3a4 4 0 018 0v4m-4 6v6m-4-6h8m-8 0H4a2 2 0 00-2 2v4a2 2 0 002 2h16a2 2 0 002-2v-4a2 2 0 00-2-2h-4"/>
                </svg>
              </div>
              <div class="text-2xl font-bold text-gray-900">{companyData.founded ? new Date().getFullYear() - companyData.founded : '10+'}</div>
              <div class="text-gray-600">Years</div>
            </div>
            <div class="text-center">
              <div class="w-16 h-16 mx-auto mb-3 bg-orange-50 rounded-2xl flex items-center justify-center border border-orange-100">
                <svg class="w-8 h-8 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.5 2.5 0 11-5 0 2.5 2.5 0 015 0z"/>
                </svg>
              </div>
              <div class="text-2xl font-bold text-gray-900">{actualReviewCount}+</div>
              <div class="text-gray-600">Clients</div>
            </div>
          </div>
        </div>

        <!-- Right Column: Contact Card -->
        <div class="lg:col-span-1">
          <div class="bg-white rounded-3xl p-8 shadow-xl border border-gray-200">
            <h3 class="text-2xl font-bold text-gray-900 mb-6 text-center">Get Free Consultation</h3>
            
            <div class="space-y-4">
              <a href={`mailto:${companyData.email}`} 
                 class="w-full bg-gradient-to-r from-red-500 to-blue-500 text-white py-4 px-6 rounded-2xl font-semibold hover:from-red-600 hover:to-blue-600 transition-all duration-300 flex items-center justify-center gap-3 shadow-lg hover:shadow-xl transform hover:-translate-y-1">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                </svg>
                Contact Now
              </a>
              
              {companyData.phone && (
                <a href={`tel:${companyData.phone}`} 
                   class="w-full bg-gray-100 text-gray-900 py-4 px-6 rounded-2xl font-semibold hover:bg-gray-200 transition-all duration-300 flex items-center justify-center gap-3 border border-gray-200">
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                  </svg>
                  {companyData.phone}
                </a>
              )}

              {companyData.website && (
                <a href={companyData.website} target="_blank" rel="noopener noreferrer"
                   class="w-full bg-gray-100 text-gray-900 py-4 px-6 rounded-2xl font-semibold hover:bg-gray-200 transition-all duration-300 flex items-center justify-center gap-3 border border-gray-200">
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9v-9m0-9v9"/>
                  </svg>
                  Visit Website
                </a>
              )}
            </div>

            <!-- Trust Badges -->
            <div class="mt-8 pt-6 border-t border-gray-200">
              <div class="grid grid-cols-2 gap-4 text-center">
                <div>
                  <div class="text-2xl font-bold text-gray-900">{actualRating}/5</div>
                  <div class="text-gray-600 text-sm">Rating</div>
                </div>
                <div>
                  <div class="text-2xl font-bold text-gray-900">{actualReviewCount}+</div>
                  <div class="text-gray-600 text-sm">Reviews</div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Customer Reviews Banner -->
  <section class="py-20 bg-gradient-to-r from-blue-600 via-purple-600 to-red-500">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold text-white mb-4">What Our Clients Say</h2>
        <div class="flex items-center justify-center gap-3 mb-6">
          <div class="flex text-yellow-400">
            {Array.from({length: 5}, (_, i) => (
              <svg class={`w-8 h-8 ${i < Math.floor(actualRating) ? 'fill-current' : 'fill-white/30'}`} viewBox="0 0 20 20">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
              </svg>
            ))}
          </div>
          <span class="text-3xl font-bold text-white">{actualRating}</span>
          <span class="text-white/90 text-xl">from {actualReviewCount} reviews</span>
        </div>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {reviewsToDisplay.slice(0, 6).map((review: FormattedReview, index: number) => (
          <div class="bg-white/10 backdrop-blur-lg rounded-2xl p-6 shadow-xl border border-white/20 hover:bg-white/20 transition-all duration-300">
            <div class="flex items-center gap-4 mb-4">
              <div class="w-12 h-12 bg-gradient-to-br from-orange-400 to-red-500 rounded-full flex items-center justify-center">
                <span class="text-white font-bold text-lg">
                  {review.author.charAt(0).toUpperCase()}
                </span>
              </div>
              <div>
                <div class="font-semibold text-white">{review.author}</div>
                <div class="flex text-yellow-400">
                  {Array.from({length: 5}, (_, i) => (
                    <svg class={`w-4 h-4 ${i < review.rating ? 'fill-current' : 'fill-white/30'}`} viewBox="0 0 20 20">
                      <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                    </svg>
                  ))}
                </div>
              </div>
            </div>
            <p class="text-white/90 leading-relaxed">{review.text}</p>
            {review.date && (
              <div class="text-sm text-white/60 mt-3">{review.date}</div>
            )}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- FAQ Section for SEO and LLM Optimization -->
  <section class="py-20 bg-gray-50">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-4xl font-bold text-gray-900 mb-4">Frequently Asked Questions</h2>
          <p class="text-xl text-gray-600">Everything you need to know about {companyData.name}</p>
        </div>

        <div class="space-y-6">
          {faqData.map((faq, index) => (
            <div class="bg-white rounded-2xl shadow-lg border border-gray-200 overflow-hidden">
              <div class="p-6">
                <h3 class="text-xl font-semibold text-gray-900 mb-3 flex items-center gap-3">
                  <div class="w-8 h-8 bg-gradient-to-br from-red-500 to-blue-500 rounded-full flex items-center justify-center text-white font-bold text-sm">
                    Q
                  </div>
                  {faq.question}
                </h3>
                <div class="pl-11">
                  <p class="text-gray-700 leading-relaxed">{faq.answer}</p>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="max-w-6xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-4xl font-bold text-gray-900 mb-4">Our Services</h2>
          <p class="text-xl text-gray-600">Comprehensive relocation solutions for your move to Switzerland</p>
        </div>
        
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
          {companyServices.map((service: any) => (
            <div class="group bg-white rounded-2xl shadow-lg border border-gray-200 p-8 hover:shadow-2xl transition-all duration-300 hover:-translate-y-2">
              <div class="w-16 h-16 bg-gradient-to-br from-red-500 to-blue-500 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
                <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
              </div>
              <h3 class="text-xl font-semibold text-gray-900 mb-4">{service.data.name}</h3>
              <p class="text-gray-600 mb-6 leading-relaxed">{service.data.description}</p>
              <a href={`/services/${service.data.id}`} 
                 class="inline-flex items-center gap-2 text-red-600 hover:text-red-700 font-medium group-hover:gap-3 transition-all duration-300">
                Learn more
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                </svg>
              </a>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Coverage Areas -->
  <section class="py-20 bg-gradient-to-br from-gray-50 to-blue-50">
    <div class="container mx-auto px-4">
      <div class="max-w-6xl mx-auto">
        <div class="text-center mb-16">
          <h2 class="text-4xl font-bold text-gray-900 mb-4">Coverage Areas</h2>
          <p class="text-xl text-gray-600">We serve major cities and regions across Switzerland</p>
        </div>
        
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
          {companyRegions.map((region: any) => (
            <div class="group bg-white rounded-2xl shadow-lg border border-gray-200 p-8 hover:shadow-2xl transition-all duration-300 hover:-translate-y-2">
              <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-purple-500 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
                <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                </svg>
              </div>
              <h3 class="text-xl font-semibold text-gray-900 mb-4">{region.data.name}</h3>
              <p class="text-gray-600 mb-6 leading-relaxed">{region.data.description}</p>
              <a href={`/regions/${region.data.id}`} 
                 class="inline-flex items-center gap-2 text-blue-600 hover:text-blue-700 font-medium group-hover:gap-3 transition-all duration-300">
                Explore region
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                </svg>
              </a>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Company Content -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <div class="prose prose-lg max-w-none prose-headings:text-gray-900 prose-p:text-gray-700 prose-a:text-red-600 prose-strong:text-gray-900">
          <Content />
        </div>
      </div>
    </div>
  </section>

  <!-- Final CTA Section -->
  <section class="py-20 bg-gradient-to-br from-red-500 via-purple-500 to-blue-600 relative overflow-hidden">
    <!-- Background Elements -->
    <div class="absolute inset-0">
      <div class="absolute top-0 left-0 w-full h-full bg-black/20"></div>
      <div class="absolute top-10 right-10 w-72 h-72 bg-white/10 rounded-full blur-3xl"></div>
      <div class="absolute bottom-10 left-10 w-96 h-96 bg-white/5 rounded-full blur-3xl"></div>
    </div>

    <div class="relative container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center text-white">
        <h2 class="text-5xl font-bold mb-6">Ready to Start Your Swiss Journey?</h2>
        <p class="text-xl mb-8 text-white/90">Contact {companyData.name} today for expert relocation assistance and make your move to Switzerland seamless.</p>
        
        <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
          <a href={`mailto:${companyData.email}`} 
             class="bg-white text-red-600 py-4 px-8 rounded-2xl font-bold text-lg hover:bg-gray-100 transition-all duration-300 flex items-center gap-3 shadow-2xl hover:shadow-3xl transform hover:-translate-y-1">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
            Get Free Consultation
          </a>
          
          {companyData.phone && (
            <a href={`tel:${companyData.phone}`} 
               class="bg-white/20 backdrop-blur-sm text-white py-4 px-8 rounded-2xl font-bold text-lg hover:bg-white/30 transition-all duration-300 flex items-center gap-3 border border-white/30">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
              </svg>
              Call Now
            </a>
          )}
        </div>

        <!-- Contact Details -->
        <div class="mt-12 pt-8 border-t border-white/20">
          <div class="grid md:grid-cols-3 gap-8 text-center">
            <div class="flex flex-col items-center gap-3">
              <div class="w-12 h-12 bg-white/20 backdrop-blur-sm rounded-full flex items-center justify-center">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                </svg>
              </div>
              <div>
                <div class="font-semibold">Email</div>
                <div class="text-white/80">{companyData.email}</div>
              </div>
            </div>
            
            {companyData.phone && (
              <div class="flex flex-col items-center gap-3">
                <div class="w-12 h-12 bg-white/20 backdrop-blur-sm rounded-full flex items-center justify-center">
                  <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                  </svg>
                </div>
                <div>
                  <div class="font-semibold">Phone</div>
                  <div class="text-white/80">{companyData.phone}</div>
                </div>
              </div>
            )}
            
            {companyData.website && (
              <div class="flex flex-col items-center gap-3">
                <div class="w-12 h-12 bg-white/20 backdrop-blur-sm rounded-full flex items-center justify-center">
                  <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9v-9m0-9v9"/>
                  </svg>
                </div>
                <div>
                  <div class="font-semibold">Website</div>
                  <div class="text-white/80">Visit Online</div>
                </div>
              </div>
            )}
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>