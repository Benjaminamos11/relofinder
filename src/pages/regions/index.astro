---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const regions = await getCollection('regions');

// Sort regions by featured first, then by population
const sortedRegions = regions.sort((a, b) => {
  if (a.data.featured && !b.data.featured) return -1;
  if (!a.data.featured && b.data.featured) return 1;
  
  // Handle population being either string or number
  const aPopulation = typeof a.data.population === 'string' ? parseInt(a.data.population.replace(/,/g, '')) : (a.data.population || 0);
  const bPopulation = typeof b.data.population === 'string' ? parseInt(b.data.population.replace(/,/g, '')) : (b.data.population || 0);
  
  return bPopulation - aPopulation;
});

const featuredRegions = regions.filter(region => region.data.featured);
// Remove featured regions from the all regions list to avoid duplication
const nonFeaturedRegions = regions.filter(region => !region.data.featured);

// Page metadata
const title = 'Swiss Regions Guide for Expats | ReloFinder.ch';
const description = 'Explore regions across Switzerland to find your ideal location, with detailed information on languages, industries, and quality of life for expats.';
---

<Layout title={title} description={description}>
  <!-- Hero Section -->
  <section class="relative py-20 bg-gradient-to-br from-primary-50 via-white to-secondary-50 overflow-hidden">
    <div class="absolute inset-0 opacity-30">
      <div class="absolute inset-0 bg-repeat" style="background-image: url(data:image/svg+xml,%3Csvg%20width%3D%2260%22%20height%3D%2260%22%20viewBox%3D%220%200%2060%2060%22%20xmlns%3D%22http%3A//www.w3.org/2000/svg%22%3E%3Cg%20fill%3D%22none%22%20fill-rule%3D%22evenodd%22%3E%3Cg%20fill%3D%22%23e11d48%22%20fill-opacity%3D%220.05%22%3E%3Ccircle%20cx%3D%2230%22%20cy%3D%2230%22%20r%3D%224%22/%3E%3C/g%3E%3C/g%3E%3C/svg%3E);"></div>
        </div>

    <div class="container relative z-10">
      <div class="text-center max-w-4xl mx-auto">
        <!-- Breadcrumb -->
        <nav class="flex items-center justify-center space-x-2 text-sm text-gray-600 mb-8">
          <a href="/" class="hover:text-primary-600 transition-colors">Home</a>
          <span>→</span>
          <span class="text-gray-900">Regions</span>
        </nav>

        <!-- Badge -->
        <div class="inline-flex items-center px-4 py-2 rounded-full bg-secondary-100 text-secondary-700 font-medium text-sm mb-8">
          <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M5.05 4.05a7 7 0 119.9 9.9L10 18.9l-4.95-4.95a7 7 0 010-9.9zM10 11a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd"/>
                </svg>
          26 Swiss Cantons & Cities
        </div>

        <!-- Title & Description -->
        <h1 class="text-4xl md:text-6xl font-bold mb-6">
          Discover Swiss <span class="gradient-text">Regions</span>
        </h1>
        <p class="text-xl text-gray-600 leading-relaxed mb-8">
          Explore Switzerland's diverse regions and find the perfect location for your new home. From cosmopolitan cities to Alpine villages, discover what makes each area unique.
        </p>

        <!-- CTA -->
        <button onclick="openModal('individual-lead-modal')" class="btn-primary px-8 py-4">
          Get Location Recommendations
        </button>
      </div>
    </div>
  </section>

  <!-- Featured Regions -->
  {featuredRegions.length > 0 && (
    <section class="py-20 bg-white">
    <div class="container">
        <div class="text-center mb-16">
          <span class="inline-block px-4 py-2 rounded-full bg-primary-100 text-primary-700 font-medium text-sm mb-4">
            Most Popular
          </span>
          <h2 class="text-3xl md:text-4xl font-bold mb-6">
            Top Expat <span class="gradient-text">Destinations</span>
          </h2>
          <p class="text-xl text-gray-600 max-w-3xl mx-auto">
            The most popular Swiss regions for international residents and their families.
        </p>
      </div>
      
        <div class="grid lg:grid-cols-2 gap-8 mb-12">
          {featuredRegions.slice(0, 2).map((region, index) => {
            // Handle population formatting
            const population = typeof region.data.population === 'string' 
              ? region.data.population 
              : region.data.population?.toLocaleString() || 'N/A';
            
            return (
              <div class="group relative bg-white rounded-3xl shadow-lg hover:shadow-2xl transition-all duration-500 overflow-hidden">
                <!-- Background Image -->
                <div class="absolute inset-0">
                  <img 
                    src={region.data.image} 
                    alt={`${region.data.name} Switzerland`}
                    class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700"
                  />
                  <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-black/20"></div>
        </div>
        
                <!-- Content -->
                <div class="relative z-10 p-8 h-96 flex flex-col justify-end text-white">
                  <div class="mb-4">
                    <span class="inline-block px-3 py-1 bg-white/20 backdrop-blur-sm rounded-full text-sm font-medium mb-3">
                      {region.data.canton || 'Switzerland'}
                    </span>
                    <h3 class="text-3xl font-bold mb-3">{region.data.name}</h3>
                    <p class="text-lg opacity-90 mb-4">{region.data.subtitle || region.data.description.substring(0, 60) + '...'}</p>
                    <p class="opacity-80 leading-relaxed mb-6">
                      {region.data.description}
                    </p>
                  </div>

                  <!-- Stats -->
                  <div class="grid grid-cols-3 gap-4 mb-6">
                    <div class="text-center">
                      <div class="text-lg font-bold">{population}</div>
                      <div class="text-xs opacity-75">Population</div>
                    </div>
                    <div class="text-center">
                      <div class="text-lg font-bold">{region.data.relocationStats?.expatsPercentage || 'International'}</div>
                      <div class="text-xs opacity-75">Community</div>
                    </div>
                    <div class="text-center">
                      <div class="text-lg font-bold">{region.data.relocationStats?.satisfaction || 'High Quality'}</div>
                      <div class="text-xs opacity-75">Living</div>
                    </div>
                  </div>

                  <!-- CTA -->
                  <a href={`/regions/${region.data.id || region.slug}`} class="inline-flex items-center justify-center w-full py-3 bg-white/10 backdrop-blur-sm border border-white/20 rounded-xl hover:bg-white/20 transition-all duration-300 group-hover:scale-105">
                    Explore {region.data.name}
                    <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                    </svg>
                  </a>
                </div>
              </div>
            );
          })}
        </div>

        <!-- Additional Featured Regions -->
        <div class="grid md:grid-cols-3 gap-6">
          {featuredRegions.slice(2).map((region) => (
            <div class="group relative bg-white rounded-2xl shadow-sm hover:shadow-lg transition-all duration-300 overflow-hidden">
              <div class="aspect-w-16 aspect-h-10">
                <img 
                  src={region.data.image} 
                  alt={`${region.data.name} Switzerland`}
                  class="w-full h-48 object-cover group-hover:scale-105 transition-transform duration-500"
                />
              </div>
              
              <div class="p-6">
                <div class="flex items-center justify-between mb-3">
                  <h3 class="text-xl font-bold text-gray-900 group-hover:text-primary-600 transition-colors duration-300">
                    {region.data.name}
                  </h3>
                  <span class="text-xs font-medium bg-primary-100 text-primary-700 px-2 py-1 rounded-full">
                    Popular
                  </span>
                </div>
                
                <p class="text-gray-600 text-sm mb-4 line-clamp-2">
                  {region.data.description}
                </p>
                
                <div class="grid grid-cols-2 gap-4 mb-4 text-center">
                  <div>
                    <div class="text-sm font-bold text-primary-600">{region.data.relocationStats?.expatsPercentage || 'International'}</div>
                    <div class="text-xs text-gray-500">Community</div>
                  </div>
                  <div>
                    <div class="text-sm font-bold text-primary-600">{region.data.relocationStats?.avgMoveTime || 'Varies'}</div>
                    <div class="text-xs text-gray-500">Setup Time</div>
                  </div>
              </div>
                
                <a href={`/regions/${region.data.id || region.slug}`} class="block w-full text-center py-2 bg-gradient-to-r from-primary-600 to-primary-700 text-white font-medium rounded-xl hover:shadow-lg transition-all duration-300 group-hover:scale-105">
                  Learn More
                </a>
              </div>
            </div>
          ))}
      </div>
    </div>
  </section>
  )}
  
  <!-- All Regions Grid -->
  <section class="py-20 bg-gray-50">
    <div class="container">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold mb-6">
          {featuredRegions.length > 0 ? 'More Swiss ' : 'All Swiss '}
          <span class="gradient-text">Regions</span>
        </h2>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
          {featuredRegions.length > 0 
            ? 'Discover additional Swiss regions and cities to find your ideal location.'
            : 'Browse our complete collection of Swiss regions and cities to find your ideal location.'}
        </p>
      </div>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
        {(featuredRegions.length > 0 ? nonFeaturedRegions : sortedRegions).map((region) => {
          // Handle population formatting
          const population = typeof region.data.population === 'string' 
            ? parseInt(region.data.population.replace(/,/g, '')) || 0
            : (region.data.population ?? 0);
          
          return (
            <div class="bg-white rounded-2xl p-6 shadow-sm hover:shadow-lg transition-all duration-300 group hover:-translate-y-1">
              <!-- Header with Image -->
              <div class="flex items-start space-x-4 mb-4">
                <div class="w-16 h-16 rounded-xl overflow-hidden flex-shrink-0">
                  <img 
                    src={region.data.image} 
                    alt={`${region.data.name} Switzerland`}
                    class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                  />
      </div>
      
                <div class="flex-1">
                  <div class="flex items-center justify-between mb-2">
                    <h3 class="text-lg font-bold text-gray-900 group-hover:text-primary-600 transition-colors duration-300">
                      {region.data.name}
                    </h3>
                    {region.data.featured && (
                      <span class="text-xs font-medium bg-primary-100 text-primary-700 px-2 py-1 rounded-full">
                        Popular
                      </span>
                    )}
                  </div>
                  <p class="text-sm text-gray-600">{region.data.subtitle || 'Swiss Region'}</p>
                  <p class="text-xs text-gray-500 mt-1">{region.data.canton || 'Switzerland'}</p>
        </div>
      </div>
      
              <!-- Description -->
              <p class="text-gray-600 text-sm mb-4 leading-relaxed line-clamp-2">
                {region.data.description}
              </p>

              <!-- Key Stats -->
              <div class="grid grid-cols-3 gap-3 mb-4 py-3 px-4 bg-gray-50 rounded-xl">
                <div class="text-center">
                  <div class="text-sm font-bold text-primary-600">
                    {population > 1000 ? `${(population / 1000).toFixed(0)}k` : population || 'N/A'}
                  </div>
                  <div class="text-xs text-gray-500">Population</div>
                </div>
                <div class="text-center">
                  <div class="text-sm font-bold text-primary-600">{region.data.relocationStats?.expatsPercentage || 'International'}</div>
                  <div class="text-xs text-gray-500">Community</div>
                </div>
                <div class="text-center">
                  <div class="text-sm font-bold text-primary-600">{region.data.relocationStats?.satisfaction || 'Quality'}</div>
                  <div class="text-xs text-gray-500">Living</div>
        </div>
      </div>
      
              <!-- Key Industries -->
              {region.data.relocationStats?.topIndustries && Array.isArray(region.data.relocationStats.topIndustries) && region.data.relocationStats.topIndustries.length > 0 && (
                <div class="mb-4">
                  <div class="text-xs font-semibold text-gray-700 mb-2">Top Industries:</div>
                  <div class="flex flex-wrap gap-1">
                    {region.data.relocationStats.topIndustries.slice(0, 3).map((industry) => (
                      <span class="px-2 py-1 bg-secondary-100 text-secondary-700 rounded text-xs">
                        {industry}
                      </span>
                    ))}
        </div>
      </div>
              )}

              <!-- Cost Range -->
              <div class="flex items-center justify-between mb-4 text-sm">
                <span class="text-gray-600">Living Cost:</span>
                <span class="font-semibold text-gray-900">{region.data.keyFacts?.costOfLiving || 'Contact for info'}</span>
              </div>

              <!-- CTA -->
              <div class="flex space-x-2">
                <a href={`/regions/${region.data.id || region.slug}`} class="flex-1 text-center py-3 bg-gradient-to-r from-primary-600 to-primary-700 text-white font-medium rounded-xl hover:shadow-lg transition-all duration-300 group-hover:scale-105">
                  Explore
                </a>
                <button onclick="openModal('individual-lead-modal')" class="px-4 py-3 border-2 border-primary-200 text-primary-600 rounded-xl hover:bg-primary-50 transition-all duration-300">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
                  </svg>
                </button>
              </div>
        </div>
          );
        })}
      </div>
    </div>
  </section>
  
  <!-- Interactive Map Section -->
  <section class="py-20 bg-white">
    <div class="container">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold mb-6">
          Explore by <span class="gradient-text">Category</span>
        </h2>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
          Discover Swiss regions by their unique characteristics and benefits for expatriates.
        </p>
      </div>
      
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
        <!-- Economic Centers -->
        <div class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-2xl p-6 border border-blue-200">
          <div class="w-12 h-12 bg-blue-500 rounded-xl flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
            </svg>
          </div>
          <h3 class="text-lg font-bold mb-3 text-blue-900">Economic Centers</h3>
          <p class="text-blue-700 text-sm mb-4">Major financial and business hubs</p>
          <div class="space-y-2">
            {regions.filter(r => r.data.relocationStats?.topIndustries?.includes('Finance')).slice(0, 3).map(region => (
              <a href={`/regions/${region.data.id || region.slug}`} class="block text-sm text-blue-800 hover:text-blue-900 hover:underline">
                • {region.data.name}
              </a>
            ))}
              </div>
            </div>

        <!-- Tech Hubs -->
        <div class="bg-gradient-to-br from-purple-50 to-purple-100 rounded-2xl p-6 border border-purple-200">
          <div class="w-12 h-12 bg-purple-500 rounded-xl flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
          </div>
          <h3 class="text-lg font-bold mb-3 text-purple-900">Tech Hubs</h3>
          <p class="text-purple-700 text-sm mb-4">Innovation and technology centers</p>
          <div class="space-y-2">
            {regions.filter(r => r.data.relocationStats?.topIndustries?.includes('Technology')).slice(0, 3).map(region => (
              <a href={`/regions/${region.data.id || region.slug}`} class="block text-sm text-purple-800 hover:text-purple-900 hover:underline">
                • {region.data.name}
              </a>
            ))}
          </div>
        </div>
        
        <!-- International -->
        <div class="bg-gradient-to-br from-green-50 to-green-100 rounded-2xl p-6 border border-green-200">
          <div class="w-12 h-12 bg-green-500 rounded-xl flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>
          <h3 class="text-lg font-bold mb-3 text-green-900">International</h3>
          <p class="text-green-700 text-sm mb-4">Global organizations & diplomacy</p>
          <div class="space-y-2">
            {regions.filter(r => r.data.relocationStats?.topIndustries?.includes('International Organizations')).slice(0, 3).map(region => (
              <a href={`/regions/${region.data.id || region.slug}`} class="block text-sm text-green-800 hover:text-green-900 hover:underline">
                • {region.data.name}
              </a>
            ))}
          </div>
        </div>
        
        <!-- Family-Friendly -->
        <div class="bg-gradient-to-br from-amber-50 to-amber-100 rounded-2xl p-6 border border-amber-200">
          <div class="w-12 h-12 bg-amber-500 rounded-xl flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"/>
            </svg>
          </div>
          <h3 class="text-lg font-bold mb-3 text-amber-900">High Quality</h3>
          <p class="text-amber-700 text-sm mb-4">Great amenities & lifestyle</p>
          <div class="space-y-2">
            {regions.filter(r => (r.data.relocationStats?.satisfaction || 4.0) >= 4.5).slice(0, 3).map(region => (
              <a href={`/regions/${region.data.id || region.slug}`} class="block text-sm text-amber-800 hover:text-amber-900 hover:underline">
                • {region.data.name}
              </a>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>
  
  <!-- CTA Section -->
  <section class="py-20 bg-gradient-to-br from-primary-600 to-secondary-600 text-white">
    <div class="container">
      <div class="text-center max-w-4xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold mb-6">
          Not Sure Which Region is Right for You?
        </h2>
        <p class="text-xl mb-8 opacity-90">
          Get personalized recommendations based on your lifestyle, career, and family needs.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
          <button onclick="openModal('individual-lead-modal')" class="btn-white px-8 py-4">
            Get Location Recommendations
          </button>
          <a href="/chat" class="btn-outline-white px-8 py-4">
            Ask Our AI Assistant
          </a>
      </div>
      
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
          <div>
            <div class="text-3xl font-bold mb-2">{regions.length}</div>
            <div class="text-sm opacity-75">Swiss Regions</div>
          </div>
          <div>
            <div class="text-3xl font-bold mb-2">Professional</div>
            <div class="text-sm opacity-75">Expert Guidance</div>
          </div>
          <div>
            <div class="text-3xl font-bold mb-2">Quality</div>
            <div class="text-sm opacity-75">Living Standards</div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>